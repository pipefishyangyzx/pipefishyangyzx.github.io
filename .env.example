# 示例环境配置文件
# 高德（AMap）示例：
# JavaScript 前端 Key（浏览器 JS SDK）
AMAP_JS_KEY=YOUR_AMAP_JS_KEY

# 服务端 REST Key（用于 server -> REST 调用），不要将该 Key 暴露到前端
AMAP_REST_KEY=YOUR_AMAP_REST_KEY

# 可选：如果您的高德应用启用了安全码（sk），可以配置以生成签名
AMAP_SK=YOUR_AMAP_SK

# 注意：不要将真实 Key 提交到公共仓库。将 .env 文件加入 .gitignore 并在部署时通过 CI/环境变量注入。
